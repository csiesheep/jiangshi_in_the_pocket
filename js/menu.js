// Menu page. The four choices are plain <a> links, so nothing is strictly
// required here yet. Kept as a module so the page has a consistent shape and
// a place to add keyboard nav / "continue game" detection later.

// Placeholder: if a saved game exists, we could relabel Start → "Continue".
// const hasSave = localStorage.getItem("zitp:save");
